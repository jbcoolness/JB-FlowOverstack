<div class="container mt-5">
    <div class="row">
        <div class="col">
            <h1>Preguntas</h1>
        </div>
    </div>
    <hr>
    <% @questions.each do |question|%>
        <div class="row question d-flex justify-content-end">
            <div class="col-2 text center">
                <p>0</p>
                <span>Votos</span>
            </div>
            <div class="col-2 text center">
                <span>0</span>
                <span>Respuestas</span>
            </div>
            <div class="col-8">
                <h3> <%= question.title %> </h3>
                <span>Preguntó: <%= question.user.username %> <%= time_ago_in_words(question.created_at) %> </span>
            </div>
        </div>
        <hr>
    <% end %>
</div>
